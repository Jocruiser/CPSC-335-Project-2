mergeLists(list)
	if list is empty print error message and return

	while list is not empty
		for every array in list
			if array is not empty
				if first element of array is smaller than target then set target array to current array

		append the first element of targetarray to results
		pop first element of target array

		if target array is empty remove from list
		reset target array

	print results